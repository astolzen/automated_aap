# SCM Credentials. In this example, they are also used for the Registry (Gitlab) 
# WiP: Re-Defice the credential var as dictionary to create multiple credentials
# Don't forget to encrypt this file with ansible-vault once populated with passwords

credential:
  scm:
    name: << SCM Name >>
    user: << SCM User >>
    pwd: << SCM Password >>
